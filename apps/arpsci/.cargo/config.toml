[build]
# Keep the compiled artifacts inside apps/arpsci/target/ instead of the
# workspace root target/, so you can cd into this directory and run
# cargo commands without the -p flag.
target-dir = "target"
